version: '3'
services:
 mongodb:
  image: mongo
  ports:
    - "27017:27017"

  enviroment:
    - MONGO_INITDB_ROOT_USERNAME=admin
    - MONGO_INITDB_ROOT_PASSWORD=password

 mongo-express:
  image: mongo-express
  ports: 5000

 postgress:
  image: postgress
  port-300